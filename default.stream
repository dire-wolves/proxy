http {
    server {
        listen 80;

        # Route /assets to assets service
        location /assets/ {
            proxy_pass http://assets.services.svc.cluster.local;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}